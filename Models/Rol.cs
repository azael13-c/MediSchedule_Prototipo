namespace MediSchedule_Prototipo.Models
{
   
    /// Enum para los roles del sistema MediSchedule.
    /// Evita errores de tipeo y mantiene los roles controlados.
    
    public enum Rol
    {
        Paciente,
        PersonalMedico,
        Admin,
        SuperAdmin
    }
}
